---
// 1. 【打招呼区】在这里告诉首页，你要用到哪些“零件”
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import TrustBar from '../components/TrustBar.astro';
import GlobalFootprint from '../components/GlobalFootprint.astro'; // <-- 就是缺了这句
import CoreCategories from '../components/CoreCategories.astro'; // <--- 新增这行
import ManufacturingStrength from '../components/ManufacturingStrength.astro'; // <--- 新增这行

// 这里可以定义首页的 SEO 标题
const pageTitle = "Royalbudd | Global Bag Manufacturing - China R&D & SE Asia Production";
---

<Layout title={pageTitle}>
 <main>
    <Hero />
    <TrustBar />
    <GlobalFootprint />
    <CoreCategories />
    
    <ManufacturingStrength />

    <section class="py-20 text-center bg-white">
        <h2 class="text-3xl font-bold">Almost Done...</h2>
        <p class="text-gray-500 mt-4">等待构建最后的 CTA 询盘转化区</p>
    </section>
  </main>
</Layout>